version: '3.8'
services:
  app:
    build:
    ports:
      - "5000:5000"
    environment:
      - FLASK_ENV: development 

  prometheus:
    image: prom/prometheus:latest 
    ports:
      - "9090:9090"
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
